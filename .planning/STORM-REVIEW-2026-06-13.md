# STORM REVIEW — no-buy re-architecture (2026-06-13)

_3 independent reviewers, all 5.5/10. Enrichment ran on 0 targets (args bug); reviews are the payload._


## 5.5/10 — Organic traffic + AI Overview readiness of the no-buy re-architecture

**Bottom line:** The strategy is correct and the engineering is clean — leading with a no-buy verdict and demoting the buy button below a free-fix gate is the right AI-Overview hedge, because the cost-fork judgment ('most of these are a free fix, do these checks first') is genuinely the one thing AIO won't synthesize and the honesty angle is a real moat for a new domain. But the build is only about 40% landed: the data that makes the card differentiated does not exist yet. 0 of 3,071 pages carry no_buy_pct or free_checks, 63% have no cause line, and 75% have no price — so today the card degrades to a generic two-sentence boilerplate repeated across ~1,944 pages, which is the WEAKEST version of the idea and actively flirts with scaled-content/duplication risk in the sandbox. You shipped the frame and skipped the painting. The enrichment pipeline (apply-nobuy-enrichment.py, with the honesty judge) is already written — it just was never run. Highest-leverage next moves, in order: (1) run the enrichment backfill so free_checks and the cause line populate the card (respect the hard-honesty rule — qualitative when ungrounded), (2) finish the part_price backfill to restore the cost-fork and price anchor, (3) resolve the duplicate above-the-fold part anchors and the orange-QuickAnswer-vs-green-NBV hierarchy conflict, and (4) remember the card converts clicks you haven't won yet — pair it with indexation monitoring and internal-link depth, which this change did not touch. Score reflects a right-direction architecture sitting on near-empty data: 5.5/10 today, an easy 8+ once the backfill runs.

- **[CRITICAL]** The thesis depends on the SHARPENED card (no_buy_pct + free_checks), but that data does not exist on a single page. Corpus query: 0 of 3,071 money_part pages carry no_buy_pct, 0 carry free_checks. The enrichment pipeline (scripts/apply-nobuy-enrichment.py, with an adversarial honesty judge) was BUILT but never RUN. So on every live page the card degrades to its weakest branch. The 'judgment call an AI Overview will not make' is currently a generic, unfalsifiable sentence ('Most E2 codes are a cheap or $0 fix, not a failed part') that an AI Overview absolutely CAN and will reproduce. The moat you shipped is the wrapper, not the differentiated content inside it.
  - FIX: Run the enrichment backfill before claiming this is shipped. Prioritize the ~1,127 pages that already have most_likely_cause (the judge has signal to work with). Honor the hard-honesty rule: only write no_buy_pct on verdict 'apply' (grounded field-frequency), let the rest stay 'apply_qualitative' with free_checks only. Target: free_checks on 80%+ of money_part pages and no_buy_pct on the defensible subset. Until free_checks exists, the card has no unique, AIO-resistant payload.
- **[HIGH]** 63% of NBV pages (1,944 of 3,071) render with NO cause line at all because they lack most_likely_cause — the card is just 'Most [code] codes are a cheap or $0 fix. Rule that out before you spend anything.' Across ~1,944 pages that is the same two sentences with only the code token swapped. On a brand-new sandbox domain, a near-identical above-the-fold block on thousands of pages is exactly the scaled-template pattern Google's spam/helpful-content systems penalize, and it gives AIO nothing to prefer you for. The card meant to be the moat is currently the single most duplicated element on the site.
  - FIX: most_likely_cause already exists on ~1,127 money_part pages and is parseable from body Common Causes on the rest. Backfill most_likely_cause first so the card always carries the brand/code-specific culprit line, then layer free_checks. A card that says 'The usual culprit is a clogged drain filter — pull it and rinse it (free)' is differentiated; 'Most codes are a cheap fix' is not.
- **[HIGH]** Anchor + duplication hazard between QuickAnswer (orange box, top) and NoBuyVerdict (green card). QuickAnswer renders FIRST as the LCP element and its CTA points to #parts-that-may-need-replacement, while NBV's escape-hatch points to #get-the-part — two different 'see the part' anchors stacked above the fold, plus the orange QuickAnswer buttons visually compete with the green 'don't buy' message they are supposed to defer to. A panicked reader sees an orange 'View Parts' button ABOVE the green 'before you buy' card, which inverts the intended hierarchy and undercuts the no-buy framing.
  - FIX: Pick one above-the-fold part anchor (#get-the-part) and route QuickAnswer's 'View Parts' to it too. Soften or remove QuickAnswer's 'View Parts' button on money_part pages so the green NBV is the unambiguous first verdict; the part CTA should only re-appear after the free-fix gate, which is the whole point of the re-architecture.
- **[HIGH]** AIO-specific structured-data surface is missing the highest-leverage hooks for THIS exact card. There is no Speakable schema, and the NBV's distinctive claim ('most are a free/cheap fix, do these checks first') is not expressed in any machine-readable form — it lives only in prose with data-pagefind-ignore on the aside, which also hides it from the site's own search. The FAQPage schema is good but its answers are templated boilerplate ('The parts that most often fix... are listed in the parts section'), which is thin and self-referential — weak AIO citation bait.
  - FIX: Add a content-derived FAQ entry that encodes the no-buy judgment as a real Q/A ('Do I need to replace the [part] for [brand] [code]? Often no — [free_check]. Replace it only if [condition].'). That gives AIO an extractable, opinionated answer that a generic overview will not generate. Drop the templated 'see the parts section' FAQ answers; they add scaled-content risk with no citation value. Reconsider data-pagefind-ignore on the NBV if you want the verdict discoverable on-site.
- **[MEDIUM]** 75% of pages (2,292 of 3,071) have no part_price, so both the NBV cost-fork ('A new X runs about $Y') and the PartCallout price anchor silently fall back to 'is not cheap' / no price. The cost-fork is the emotional engine of the no-buy verdict — without a real dollar figure, 'is not cheap' is vague and far less persuasive, and the price anchor that pre-qualifies the buyer is gone. backfill-part-price.py exists for this and has only reached ~25% coverage.
  - FIX: Finish the part_price backfill to 80%+. Price ranges for common appliance parts (drain pump $40-90, water inlet valve $25-60, hot surface igniter $20-45, control board $120-300) are well-established and defensible. This is the lowest-risk, highest-conversion-impact backfill available and it feeds both components at once.
- **[MEDIUM]** Sandbox-phase reality check: on a 6-12 month new domain competing for '[brand] [appliance] [code]', the binding constraint is not the above-the-fold card — it is whether these pages get crawled, indexed, and earn ANY external trust signal. Nothing in the reviewed change touches internal-link depth to deep pages, indexation monitoring, or off-site signals. A perfect no-buy card on a page that sits in Crawled-not-indexed earns zero clicks. The re-architecture optimizes conversion of a click the site may not yet be winning.
  - FIX: Pair the card work with: (1) tighten internal linking so every code page is <=3 clicks from home via the brand hubs that already exist; (2) instrument GSC indexation coverage and watch the indexed-page curve, not just the card; (3) seed a handful of genuinely link-worthy hub assets (per-brand 'most codes are a free fix' data tables built from the same no_buy_pct data) to earn the first external links. The honesty angle is a real differentiator — but it only converts traffic you have earned.
- **[LOW]** The independent-note ('We don't sell parts, so we tell you when not to buy one') is a strong, distinctive trust line, but it is repeated verbatim on every page and rendered as small brand-colored text just below the meta row, immediately above QuickAnswer. It risks reading as a templated tagline rather than an earned claim, and it competes for the same eye-zone as the NBV card that proves it.
  - FIX: Keep the line (it is good honesty positioning per the hard-honesty rule) but consider tying it visually to the NBV card itself (e.g., as the card's footnote) rather than as a separate repeated banner, so the claim and the proof are adjacent. Low priority versus the data backfills.

## 5.5/10 — Content accuracy + honesty integrity (no-buy verdict re-architecture)

**Bottom line:** The re-architecture is strong where it's true and dangerous where it's blind. The card is genuinely defensible and a real trust-and-conversion moat for drain (212 pages) and fill/inlet (94) codes, where a clog or kink is the dominant field cause — that's the LG OE / Samsung 3C sweet spot and those pages are excellent. The fatal flaw is structural: the 'most are a cheap/$0 fix' lead is OPT-OUT (renders unless diy_or_pro === 'pro'), and 1,847 of 3,071 money_part pages have no diy_or_pro field, so they ship the claim with no editorial judgment. That mislabels whole classes where the code genuinely IS the part — refrigerant/sealed-system (Daikin U4, Mitsubishi U3), compressors/inverter boards (Daikin E8, Mitsubishi U2, Panasonic H98/H99), igniters/heating (AirEase E1, contradicting its own body), control boards, safety circuits (ABB FF63 STO), and the inherited industrial VFD corpus. On those, 'most likely a $0 fix' is false, occasionally unsafe, and burns the exact trust the card is meant to win. Good news on the honesty rule: no_buy_pct and free_checks are populated on zero pages, so nothing fabricated has shipped — but the same class-blind default will turn a wrong qualitative claim into a wrong precise percentage the moment backfill starts. Fix order: (1) flip the claim to opt-in so unknown/unlabeled pages render the neutral lead; (2) hard-guard compressor/refrigerant/inverter/igniter/control-board/safety money_parts to pro so they can never render cheap-fix; (3) align the card lead with each page's own most_likely_cause; (4) gate no_buy_pct and free_checks behind the same class and safety filters. Files: C:\\Users\\chris\\industrial-fixes\\src\\components\\NoBuyVerdict.astro (lines 47-54 lead logic), C:\\Users\\chris\\industrial-fixes\\src\\layouts\\PostDetails.astro (lines 330-338 prop wiring; pro={diy_or_pro === 'pro'} is the single point to fix).

- **[CRITICAL]** The 'most are a cheap/$0 fix' claim renders by DEFAULT and is false for whole code classes. In PostDetails.astro the NoBuyVerdict shows the buyer-deterrent lead ('Most {code} codes are a cheap or $0 fix, not a failed part') whenever diy_or_pro !== 'pro'. But 1,847 of the 3,071 money_part pages have NO diy_or_pro field at all, so they silently fall into the cheap-fix claim with zero editorial judgment. Concrete false cases I confirmed: Daikin U4 and Mitsubishi U3 (money_part 'Refrigerant charge' — EPA-licensed recover/recharge, never a $0 DIY fix), Daikin E8 ('inverter compressor'), Mitsubishi U2 ('inverter PCB'), Manitowoc ice machine E01 ('refrigerant charge'), Panasonic microwave H98/H99 ('inverter board'). Telling a reader a refrigerant-leak or inverter-compressor code is 'most likely a $0 fix' is factually wrong and erodes the exact trust moat the card exists to build.
  - FIX: Make the cheap-fix claim OPT-IN, not opt-out. Treat a missing diy_or_pro as 'unknown' and render the neutral/pro lead ('this code can come from several parts; confirm the cause before you buy'), NOT the cheap-fix assertion. Only show 'most are a cheap/$0 fix' when an explicit, per-page editorial flag affirms it (e.g. cheap_fix_likely: true, or no_buy_pct present). Backfill diy_or_pro/pro across the 1,847 unlabeled pages before they keep shipping the default claim.
- **[CRITICAL]** Sealed-system, compressor, and safety-circuit codes render the cheap-fix card. sealed/compressor class shows ~20 cheap-fix cards despite 0 marked DIY. ABB ACS580 FF63 (money_part 'control board assembly') is a Safe Torque Off safety-diagnostics software fault — the page body and ABB's own docs say reboot once then call ABB, and a failing board 'can cause unsafe behavior in the STO safety circuit.' Putting a green 'most of these are a $0 fix' card above the fold on a functional-safety fault is both wrong and a liability. Same pattern on industrial VFD faults inherited from the original industrial-fixes corpus (ABB ACS550/ACS580 overcurrent, phase-loss, etc.), which are not appliance cheap-fix territory.
  - FIX: Force diy_or_pro: 'pro' (or a dedicated safety/sealed-system flag) for every code whose money_part is a compressor, refrigerant charge, sealed-system component, inverter/IPM board, or any safety circuit (STO, ground-fault, gas safety lockout). These must NEVER render the cheap-fix lead. Add a keyword guard in the build so a money_part matching compressor|refrigerant|sealed|inverter|STO can't fall through to the non-pro branch.
- **[HIGH]** Heating/no-heat and control-board codes contradict the card's own page body. AirEase furnace E1 (money_part 'Hot surface igniter') renders 'Most E1 codes are a cheap or $0 fix' while the body states 'The igniter is the most common single point of failure.' Across the corpus: heating/no-heat shows 110 cheap-fix cards (only 21 explicit DIY, 89 unlabeled) and control-board shows 218. Heating elements, igniters, gas valves, and control boards are usually the part. There is often ONE legitimate free check (clean the flame sensor, reseat a connector, power-cycle), but leading with 'most are a $0 fix' overstates it and is contradicted by the page's stated #1 cause.
  - FIX: For heating/igniter/element/gas-valve/control-board money_parts, default to the neutral lead and reserve the cheap-fix assertion for codes where a free fix genuinely dominates (e.g. dirty flame sensor as documented #1 cause). Where a free check exists but the part is the likely culprit, reframe to 'do this one free check first, then it's usually the {part}' rather than 'most are a $0 fix.' Align the card lead with the body's most_likely_cause so they never contradict.
- **[HIGH]** Door-lock class is mislabeled toward cheap-fix. door-lock shows 266 cheap-fix cards (99 DIY, 167 unlabeled). Door-lock/latch errors are frequently the lock actuator or latch assembly itself (a real part), not a free fix — the most_likely_cause on several is literally the latch/lock assembly. The 'most are a $0 fix' lead is false for the subset where the assembly has failed.
  - FIX: Audit the door-lock corpus: where most_likely_cause is the lock/latch assembly (the part), drop the cheap-fix lead to neutral. Keep cheap-fix only where the dominant cause is a free action (door not fully shut, obstruction, reseat connector, reset).
- **[MEDIUM]** no_buy_pct and free_checks are populated on ZERO pages, so no fabricated numbers have shipped (good, and consistent with Chris's hard-honesty rule). But the qualitative default IS the unverified claim — the honesty risk is already live without a single number. When no_buy_pct backfill begins, the same class-blind default will turn a wrong qualitative claim into a wrong PRECISE claim (e.g. 'About 70% of U4 codes are a $0 fix'), which is worse.
  - FIX: Gate no_buy_pct population behind the same class guard: never assign a high no_buy_pct to sealed-system, compressor, igniter, heating-element, control-board, or safety codes. Require the per-page field-frequency estimate to be defensible from the page's own documented cause distribution (the LG OE page already does this well with ~45%/~25%/... cause weights — derive no_buy_pct from those, not from a guess). Return null and stay qualitative when the dominant cause is the part.
- **[MEDIUM]** Free-check safety is latent rather than tested. Today the card falls back to 'work the free checks in the steps below' because free_checks is empty, so no explicit unsafe step ships yet. But the steps it points to include live-voltage multimeter testing, gas-valve checks, and pump access. When free_checks arrays are authored, there is no guardrail preventing an unsafe instruction (e.g. 'test the igniter for continuity', 'check gas pressure') from being framed as a casual free first step above the fold.
  - FIX: Add a safety filter for free_checks authoring: only no-tool, no-panel-removal, power-on-safe actions qualify (clean filter, straighten hose, reseat external connector, power-cycle, confirm valve open). Anything requiring panel removal, a multimeter, gas work, or live voltage must NOT appear in free_checks — it belongs in the gated step-by-step with the existing unplug-first warnings.

## 5.5/10 — Revenue impact of the no-buy verdict re-architecture (monetized affiliate clicks per engaged session)

**Bottom line:** The strategy is directionally right (trust-first leads can lift mC/1000 on high-intent repair traffic), but as SHIPPED it is not delivering it — and is probably net-negative right now. The whole thesis rests on the card being specific ('70% are a $0 filter clog, do these 2 checks first'), yet no_buy_pct and free_checks exist in 0 of 5,082 posts and part_price in only 779. So every live card renders in its weakest, check-less, often price-less form: it plants doubt and adds a scroll above the buy path without delivering the concrete free-fix payoff that earns the trust or pre-qualifies the buyer. Compounding this, the 'buy button moved below the gate' claim is mostly cosmetic — 5,061 posts still render orange Amazon buy buttons inline in the article body, above the gate and below the doubt card, so intent isn't actually gated, it's just preceded by discouragement. The decided buyer (your highest-converting visitor) is handed only a small text skip-link that jumps past those buttons. Net: you shipped the cost of the strategy (intent dampening, extra scroll, tone clash) to 100% of pages while the benefit (specific honest fork + warm pre-qualified click) reaches ~0%. Highest-leverage fixes, in order: (1) gate the card to render only when free_checks is populated, then backfill checks/price/pct on top-traffic pages and A/B before any global rollout; (2) decide whether the gate is real and treat the in-content Amazon buttons consistently; (3) co-locate a price anchor with the card and upgrade the escape hatch to a real button. Do these and this becomes a 7.5-8. Today, as a blind global ship with empty supporting fields, it is a 5.5.

- **[CRITICAL]** The revenue thesis depends on fields that exist in ZERO live posts. no_buy_pct appears in 0 of 5,082 posts and free_checks in 0 of 5,082. The card's own code paths confirm this is its weakest mode: with no free_checks it prints 'Work the free checks in the steps below before you order anything' (a vague deferral, no concrete free action) and with no no_buy_pct it prints 'Most these codes are a cheap or $0 fix.' So on 100% of live pages the reader sees an unquantified, check-less 'don't buy yet' card ABOVE the buy path. That is the worst of both worlds: it dampens buy intent (plants doubt, adds a scroll) without delivering the specific free-fix payoff that earns the trust and makes the eventual click convert harder. The pre-qualifier mechanism the design banks on literally cannot fire because there are no checks in the card to rule out. This single gap likely makes the current shipped state net-negative vs the old above-fold orange button.
  - FIX: Do not ship the no-buy card in its check-less form at scale. Either (a) gate the NoBuyVerdict to render ONLY when free_checks is non-empty (so 0 pages today, then backfill), keeping the orange PartCallout above-fold everywhere else, or (b) backfill free_checks for the top-traffic money_part pages first and roll the card out only there. Generate free_checks/no_buy_pct from established repair knowledge per the honesty rule (null when unsure). A/B the card against the old layout on a traffic slice before global rollout — right now it is shipped blind to every page.
- **[CRITICAL]** The 'buy button moved below the gate' framing is largely illusory for the dominant revenue surface. 5,061 of 5,082 posts contain inline amazon.com links in the article BODY, and PostDetails.astro styles every one into an orange CTA button (#article a[href*=amazon.com]). Those buttons sit between the NoBuyVerdict and the moved PartCallout at #get-the-part, i.e. ABOVE the gate. So the highest-volume monetized surface was never gated. The re-architecture moved one card (PartCallout) below the fix steps while the in-content Amazon buttons still capture intent above it. The net effect is not 'buy intent is gated' — it is 'the green doubt card now sits above a still-ungated wall of orange in-content buy buttons,' which mostly adds friction and a tone clash rather than the clean pre-qualification the design assumes.
  - FIX: Decide whether the gate is real. If the strategy is genuine pre-qualification, the in-content Amazon buttons need the same treatment (or the NoBuyVerdict needs to acknowledge them). If the strategy is actually 'lead with trust, keep buy paths available,' then stop describing PartCallout's move as a gate and instead measure mC/1000 with the card present vs absent. Instrument the in-content Amazon button clicks by placement (they already flow through the analytics listener) so you can see whether above-gate buy clicks rose or fell after the card shipped.
- **[HIGH]** The reader who already knows they need the part is served only by a text skip-link ('Already ruled those out? See the exact part →') to #get-the-part. That is weak for the highest-value, highest-intent visitor. The link is small (0.85rem, brand-colored, bottom-border underline), sits at the BOTTOM of the green card after 2-4 lines of don't-buy copy, and — critically — jumps PAST the in-content Amazon buttons to the PartCallout. On a returning/decided buyer this is a worse experience than the old above-fold orange button: they must read past discouragement, find a quiet link, and trust an anchor jump. Decided buyers are the ones who convert at the highest rate; adding any friction to them is the most expensive friction on the page.
  - FIX: Make the escape hatch a real button, not an inline link, and raise it visually (secondary outline style, not a text link). Consider rendering it inline with the lead so a decided buyer sees it in the first 1.5s without reading the discouragement. Better: keep a compact price-anchored 'See the part — $X' affordance visible for decided buyers regardless, so the card complements rather than blocks. The mobile sticky bar partially covers this on phones but not on desktop.
- **[HIGH]** Mobile sequencing creates a discouragement-heavy first viewport with no buy affordance, and the sticky bar's 640px scroll trigger may not rescue it. On mobile the order is QuickAnswer → NoBuyVerdict (green, don't-buy) → brand-hub link → AtAGlance → EmailCapture → AffiliateDisclosure → article. The StickyPartsBar only appears after scrollY > 640px. On a tall phone the NoBuyVerdict + email capture can push the first monetized CTA (and the sticky trigger) far down, so the reader's first full screen is doubt + an email ask, no way to buy. The card and the sticky bar do not visually coordinate (green 'don't buy' card vs orange sticky 'See prices' that can appear within the same scroll), which reads as mixed signals rather than a funnel.
  - FIX: Lower the sticky bar threshold (e.g. 320-400px) so the buy affordance is reachable sooner on mobile, OR trigger it when the NoBuyVerdict scrolls out of view rather than a fixed pixel count. Confirm via a real mobile viewport (browse/connect-chrome) that a monetized CTA is reachable within one or two thumb-scrolls. Consider suppressing the second EmailCapture-before-content on mobile so the doubt-then-ask-then-nothing stack is shorter.
- **[MEDIUM]** Green card vs orange button is framed as 'deliberate inverse / complement,' but with the check-less copy it competes rather than complements. A complement would be 'here's the free thing to try, and here's the part if it fails.' What ships is 'most of these are cheap/free (no specifics)' up top and a confident orange 'Find the exact part' far below — the two are separated by the entire article, so the reader never sees the honest fork and the buy option together. The trust dividend (card) and the conversion surface (button) are decoupled in space, so the card pays its full intent-dampening cost up front while the button collects none of the trust lift at the moment of doubt.
  - FIX: Co-locate a quiet price anchor with the no-buy card ('If the checks fail, the part is ~$X — jump down') so the honest fork and the buy path are visible together. This is where the green/orange pairing actually earns trust AND keeps the click warm. Requires part_price, which is missing on 2,292 of 3,071 money_part pages — backfill price first; it is a defensible street-price estimate, not fabricated precision.
- **[MEDIUM]** part_price coverage gap weakens the card's strongest lever on the majority of pages. Only 779 of 3,071 money_part pages carry part_price, so 2,292 fall back to 'A new {part} is not cheap.' That fallback is pure discouragement with no number — it tells the reader buying is expensive without giving the anchor that makes the eventual purchase feel reasonable. Price anchoring is what converts a deferred buyer later; without it the card only does the suppressing half of its job.
  - FIX: Backfill part_price (a range like $45-90) for the top money_part pages by traffic. A defensible street-price band from established repair-parts knowledge satisfies the honesty rule. Until then, suppress the 'not cheap' fallback line entirely — it costs intent and returns nothing.
- **[LOW]** Both the NoBuyVerdict and the in-content Amazon buttons can present a buyer with three-to-four different merchant destinations on one page (RepairClinic via PartCallout, Amazon via in-content + PartCallout secondary, RepairClinic/PartSelect via PartsSpecialist, RepairClinic via sticky). Choice overload at the buy moment can depress click-through. The re-architecture added the card but did not consolidate the now-numerous downstream buy surfaces.
  - FIX: Audit how many distinct buy CTAs a single money_part page renders below the gate and trim to a clear primary + one alternate. Measure mC/1000, not raw click count — more buttons can raise gross clicks while lowering monetized conversion.

## SYNTHESIS

The architecture is the right move; the ship is not done. Leading with a no-buy verdict and demoting the buy button below a free-fix gate is the correct AI-Overview hedge and a genuine honesty moat for a sandbox domain, and all three reviewers independently land on the same 5.5/10 for the same reason: the data that makes the card differentiated does not exist on a single live page. I verified the corpus directly: 0 of 5,082 posts carry no_buy_pct or free_checks, only 1,129 have most_likely_cause, 779 have part_price, and 1,226 have diy_or_pro, against 3,071 money_part pages. So today every card renders in its weakest, check-less form, repeating near-identical two-sentence boilerplate across ~1,944 cause-less pages (scaled-content/duplication risk on a new domain) while paying the full cost of dampened buy intent with none of the trust payoff. Worse, the cheap-fix claim is opt-OUT (NoBuyVerdict line 41, wired at PostDetails line 337 as pro={diy_or_pro==='pro'}), so the ~1,847 pages with no diy_or_pro field ship "most of these are a $0 fix" by default onto refrigerant, compressor, inverter, and industrial-VFD safety codes where that is factually wrong and occasionally a liability (I confirmed 91 sealed-system/compressor/inverter money_part pages, including ABB ACS580/ACS880 and Allen-Bradley PowerFlex faults, that lack a pro label). Net: keep the architecture, but it is roughly 40% landed and arguably net-negative versus the old above-fold button until the data backfills run and the claim is flipped to opt-in.


### Top actions (in order):

1. Flip the cheap-fix claim from opt-out to opt-in and hard-guard dangerous classes BEFORE anything else. In NoBuyVerdict.astro, treat missing diy_or_pro as 'unknown' and render the neutral lead (not the $0-fix assertion); only assert cheap-fix when an explicit flag or no_buy_pct affirms it. Add a build-time keyword guard so any money_part matching compressor|refrigerant|sealed|inverter|IPM|STO|gas-valve|ignit forces the pro/neutral branch. This is a code-only fix that stops shipping false and unsafe claims on the ~1,847 unlabeled pages and the 91 sealed-system/VFD pages I confirmed today.
2. Run the enrichment backfill that was built but never run (scripts/apply-nobuy-enrichment.py, with its honesty judge); the enrichment output list is currently empty (0 records). Prioritize the 1,129 pages that already have most_likely_cause so the judge has signal. Honor the hard-honesty rule: write no_buy_pct only on grounded 'apply' verdicts, leave the rest 'apply_qualitative' with free_checks only, return null when ungrounded. Target free_checks on 80%+ of money_part pages and no_buy_pct on the defensible drain/fill/inlet subset where a clog or kink is the documented dominant cause. Until free_checks exists, the card has no AI-Overview-resistant payload.
3. Either gate the NoBuyVerdict to render only when free_checks is populated (0 pages today, rolling out as the backfill lands) OR roll it out only on top-traffic pages first, and A/B against the old above-fold orange button on a traffic slice before any global claim of success. Do not keep shipping the check-less card blind to 100% of pages; it costs intent and returns nothing on the majority. Instrument in-content Amazon button clicks by placement so you can actually measure monetized clicks per 1,000 engaged sessions with the card present vs absent.
4. Finish the part_price backfill from 779 to 80%+ of money_part pages and backfill most_likely_cause where it is parseable from the body Common Causes. Defensible street-price bands (drain pump $40-90, water inlet valve $25-60, hot surface igniter $20-45, control board $120-300) satisfy the hard-honesty rule and feed BOTH the cost-fork and the PartCallout price anchor at once. Suppress the bare 'is not cheap' fallback line until a real range exists. This is the lowest-risk, highest-conversion backfill available.
5. Resolve the above-the-fold hierarchy and anchor conflicts, then pair the card with the indexation work it depends on. Route QuickAnswer's 'View Parts' (currently #parts-that-may-need-replacement) to the single #get-the-part anchor and soften/remove that orange button on money_part pages so the green verdict is the unambiguous first signal; upgrade the escape hatch from a small text link to a real price-anchored button (especially on desktop, where the sticky bar is hidden by the 640px media query). Separately, tighten internal linking so every code page is <=3 clicks from a brand hub and instrument GSC indexation coverage. A perfect card on a Crawled-not-indexed page earns zero clicks, and this change touched none of the indexation/link-depth surface.

### Flagged for human eyes:

- HONESTY/SAFETY (highest): 91 confirmed sealed-system/compressor/inverter money_part pages (e.g. ABB ACS580/ACS880, Allen-Bradley PowerFlex 525/753, Amana dryer F22, A.O. Smith tankless E-codes) plus the accuracy review's named cases (Daikin U4, Mitsubishi U3/U2, Manitowoc E01, Panasonic H98/H99, ABB ACS580 FF63 Safe-Torque-Off) currently render or would render 'most are a $0 fix' above the fold. These need human eyes on the class guard before any no_buy_pct backfill touches them, and the inherited industrial-fixes VFD corpus should likely be excluded from appliance cheap-fix framing entirely.
- No enrichment slugs to review yet: the honesty-judged enrichment set is empty (0 apply/apply_qualitative/reject records) and 0 of 5,082 posts carry no_buy_pct or free_checks. Nothing fabricated has shipped, which is consistent with the hard-honesty rule. But every no_buy_pct written in the upcoming backfill needs human spot-check on the first batch, because the same class-blind default would turn a wrong qualitative claim into a wrong precise percentage.
- AirEase furnace E1 (and the heating/igniter class generally): the card's cheap-fix lead directly contradicts the page body's own stated #1 cause ('the igniter is the most common single point of failure'). Flag the card-lead-vs-body contradiction as a content-integrity check to run across the 110 heating and 218 control-board cheap-fix cards.
- Decision needed from Chris: is the gate real or cosmetic? 5,061 posts still render orange in-content Amazon buy buttons ABOVE the gate (confirmed at PostDetails.astro line 620), so buy intent is not actually gated, only preceded by the green doubt card. Either treat the in-content buttons consistently with the gate or stop describing PartCallout's move as a gate. This is a strategy call, not an autonomous fix.